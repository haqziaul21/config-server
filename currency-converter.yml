server:
  port: 8100 
  
feign:
 client:
  config:
    default:
      connectTimeout: 5000
      readTimeout: 5000
  
zia:
  feign:
    servicename: currency-exchange
    
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka